PORT=3001
CORS_ORIGIN=http://localhost:5173

# Auth (token-based for the demo dashboard).
DEMO_EMAIL=operator@wws.gov
DEMO_PASSWORD=Wws123!
AUTH_TOKEN=wws-demo-token

# Set to "true" to disable HTTP + WebSocket auth checks (e.g. local debugging).
AUTH_DISABLED=false
